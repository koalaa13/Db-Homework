update Marks as m set Mark = (select Mark from NewMarks nm where m.StudentId = nm.StudentId and m.CourseId = nm.CourseId)
where exists (select * from NewMarks as nm where m.StudentId = nm.StudentId and m.CourseId = nm.CourseId)